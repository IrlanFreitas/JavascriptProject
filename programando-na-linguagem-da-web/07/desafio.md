## Desafio 03

```html
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>03 - Desafio</title>
</head>
<body>
    <script type="text/javascript">
        /* Essa URL que tem um maravilhoso retorno, apresenta vários exemplos para testes de requests/response
         * (Test your front-end against a real API) - https://reqres.in/
         *
         * Usaremos especificamente uma lista de usuários, disponível em: https://reqres.in/api/users?page=2
         *
         * Fazendo um request para ela, obteremos um JSON, e com ele devemos:
         *
         * 1 - Monte uma tabela dinâmica (via JS), para cada usuário (`user` listado no array `data`)
         * 2 - Liste os dados na ordem apresentada:
         *   ID / First Name / Last Name / Avatar (isso é uma imagem, faça ela ser exibida)
         *
         * O `user` (usuário) de `last_name='Morris'`, deve exibir outro avatar qualquer (use uma imagem do google qualquer)
         * Com o objeto deste registro `user` (usuário) de `last_name='Morris'`, duplique esse objeto e adicione na lista
         *  Use para esse registro um nome aleatório e o antigo avatar do `user` (usuário) de `last_name='Morris'`
         *
         * **Obs:**
         * Separe em arquivos as responsabilidades e terás menos problemas, além disso (um componente/function para ajax é essencial)
         *
         */
    </script>
</body>
</html>
```